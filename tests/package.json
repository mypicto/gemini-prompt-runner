{
  "name": "gemini-prompt-runner-tests",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build-bundle.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
